// No-op: this file stubs out expo/src/winter/runtime.native
// to prevent the __ExpoImportMetaRegistry lazy getter from being installed.
// That getter fires a require() call outside Jest's module scope in Jest 30,
// causing "You are trying to import a file outside of the scope of the test code."
